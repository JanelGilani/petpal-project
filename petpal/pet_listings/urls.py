from django.urls import path
from . import views

app_name="pet_listings"
urlpatterns = [ 
    # path('all/', views.all_pets, name="all_pets"),
    
    # path('search/', views.search, name="search"),
    # path('add/', views.add_pet, name="add_pet"),
    # path('edit/<int:pet_id>/', views.edit_pet, name="edit_pet"),
    # path('delete/<int:pet_id>/', views.delete_pet, name="delete_pet"),
    # path('search/<int:pet_id>/', views.pet_detail, name="pet_detail"),
    # path('<int:pet_id>/like/', views.add_to_collection, name="add_to_collection"),
    # path('<int:pet_id>/unlike/', views.remove_from_collection, name="remove_from_collection"),
    # path('<int:pet_id>/adopt/', views.adopt, name="adopt"),

    # path('login/', views.LoginView.as_view(), name="login"),
    # path('logout/', views.logout, name="logout"),
]